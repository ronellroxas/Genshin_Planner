<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mobdeve.s13.g26.genshinplanner">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GenshinPlanner">
        <activity android:name=".activities.ViewItemActivity"/>
        <activity android:name=".activities.ItemListActivity" />
        <activity android:name=".activities.PlanListActivity" />
        <activity android:name=".activities.ViewCharacterActivity" />
        <activity android:name=".activities.CharacterListActivity" />
        <activity android:name=".activities.ViewProfileActivity" />
        <activity android:name=".activities.RegisterActivity" />
        <activity android:name=".activities.HomeActivity" />
        <activity android:name=".activities.CreatePlanActivity"/>
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>